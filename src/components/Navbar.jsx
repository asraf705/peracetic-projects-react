import React from "react";

const Navbar = () => {
  return (
    <div className="h-16 bg-gradient-to-r from-cyan-500 to-blue-500">
      <nav>
        <h2>
          <span className="text-gray-50">Nav bar</span>
        </h2>
      </nav>
    </div>
  );
};

export default Navbar;
